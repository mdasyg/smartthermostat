<div id="device-form">
  <%= form_for device do |device_form| -%>

    <% if device.errors.any? %>
      <div id="error-explanation">
        <h2><%= pluralize(device.errors.count, 'error') %> prohibited this
          device from being saved:</h2>

        <ul>
          <% device.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row">

      <div class="col-xs-6">
        <div class="field form-group">
          <%= device_form.label :name %>
          <%= device_form.text_field :name, class: 'form-control' %>
        </div>
      </div>

      <div class="col-xs-6">
        <div class="field form-group">
          <%= device_form.label :location %>
          <%= device_form.text_field :location, class: 'form-control' %>
        </div>
      </div>

    </div>

    <div class="row">

      <div class="col-xs-12">
        <div class="field form-group">
          <%= device_form.label :description %>
          <%= device_form.text_area :description, class: 'form-control' %>
        </div>
      </div>

    </div>

    <hr/>

    <div class="field form-group">
      <button type="button" class="add-new-device-property-button form-control">
        <span>Add New Device Property</span>
      </button>
    </div>

    <%= render partial: 'device_property_form', locals: { properties: device.properties } %>

    <hr/>

    <div class="actions">
      <%= device_form.submit %>
    </div>

  <% end -%>

  <div id="device-property-template" class="hidden">
    <%= render partial: 'device_property_form_element', locals: { device_property: Property.new } %>
  </div>
</div>
