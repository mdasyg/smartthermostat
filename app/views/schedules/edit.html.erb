<% provide(:title, 'Edit Schedule') %>

<div class="well">
  <h1>Editing Schedule</h1>
</div>

<%= render 'schedule_form', schedule: @schedule, repeat_every_list: @repeat_every_list %>

<hr/>

<%= link_to 'Show', @schedule %> |
<%= link_to 'Back', schedules_path %>
